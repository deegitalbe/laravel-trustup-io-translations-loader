services:
  cli:
    build:
      context: .
      target: cli
      dockerfile: Dockerfile
    image: "laravel-trustup-io-translations-loader-php-local"
    volumes:
      - ./:/opt/apps/app
  bun:
    build:
      context: .
      target: bun
      dockerfile: Dockerfile
    image: "laravel-trustup-io-translations-loader-bun-local"
    volumes:
      - ./:/opt/apps/app
